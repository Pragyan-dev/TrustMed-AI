[pytest]
markers =
    integration: marks tests that require live services (Neo4j, FastAPI backend, LLM API)
    slow: marks tests that take more than 30 seconds
    unit: marks pure unit tests with no external dependencies
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
